{
  "name": "procureos-web",
  "version": "1.0.0",
  "private": true,
  "description": "ProcureOS Web — monorepo (server Express + admin React)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix admin install",
    "build": "npm --prefix admin run build",
    "start": "node server/src/index.js"
  }
}
